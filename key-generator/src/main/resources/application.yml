micronaut:
  application:
    name: babyUrlGenerator
cassandra:
  default:
    clusterName: '"Test Cluster"'
    contactPoint: '"127.0.0.1"'
    port: 9042
    maxSchemaAgreementWaitSeconds: 20
    ssl: true
